[project]
name = "app"
version = "2.0.0"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "--tb=line -q --no-header"

[tool.setuptools]
package-dir = {"" = "src"}