You can put any file in this directory that includes some tests.

If function starts with `test_` or ends with `_test` it will be run automatically by pytest 

Preparing tests locally `pip install --upgrade -r requirements.txt` in root directory

Running test locally `pytest`